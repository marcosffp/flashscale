# Etapa 10 (negocio.md §7.5): api fala com o Postgres através do PgBouncer
# (transaction pooling), não direto — migrations passam pelo mesmo caminho.
DB_HOST=localhost
DB_PORT=6432
DB_USER=flashscale
DB_PASSWORD=flashscale
DB_NAME=flashscale
PORT=3001

# gateway
GATEWAY_PORT=3000
JWT_SECRET=troque-por-um-segredo-forte-gerado-localmente
JWT_EXPIRES_IN=15m
API_BASE_URL=http://localhost:3001

# orchestrator
ORCHESTRATOR_PORT=3002
CLUSTER_NAMESPACE=blackfriday
