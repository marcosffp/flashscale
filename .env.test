# App e migration falam com o Postgres de teste através do PgBouncer
# (transaction pooling, porta 6433) — mesmo caminho de produção, pra
# validar de verdade que prepare:false funciona sob carga concorrente.
# Detalhes: docs/06-banco-de-dados-e-pgbouncer.md
DB_HOST=localhost
DB_PORT=6433
DB_USER=flashscale
DB_PASSWORD=flashscale
DB_NAME=flashscale_test
PORT=3099
