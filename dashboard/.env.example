VITE_ORCHESTRATOR_WS_URL=ws://localhost:3002
# Usado pelo botão "Matar pod aleatório" (Etapa 8) — POST /pods/kill direto
# do browser contra o orchestrator, mesma simplificação de CORS aceita pro
# gateway no botão "Disparar carga".
VITE_ORCHESTRATOR_HTTP_URL=http://localhost:3002
VITE_GATEWAY_URL=http://localhost:3000
# Produto semeado pela migration SeedProdutoCarga1700000000002 — usado pelo
# botão "Disparar carga" (Etapa 7).
VITE_PRODUTO_CARGA_ID=11111111-1111-4111-8111-111111111111
