# Template only — no real values here, this file is versioned.
# Copy to k8s/secrets.yaml, fill in real values, and apply that file instead.
# k8s/secrets.yaml is gitignored and must never be committed.
apiVersion: v1
kind: Secret
metadata:
  name: blackfriday-secrets
  namespace: blackfriday
type: Opaque
stringData:
  POSTGRES_PASSWORD: "troque-por-uma-senha-forte-gerada-localmente"
  JWT_SECRET: "troque-por-um-segredo-forte-gerado-localmente"
